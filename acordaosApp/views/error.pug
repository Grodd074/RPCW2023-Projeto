extends layout

block content
    .w3-card-4
        header.w3-container.w3-brown
            .header-container
                h1.header-left Lista de Acordãos - ERRO 

        .w3-container
            if typeof error == 'object'
                h3 #{status} #{error.status}
                pre #{error.stack}
            else
                h3 #{error}


        footer.w3-container.w3-brown
            p #[a(href="/") Voltar]
